#This is just a tutorial project. 
#### It's mostly CRUD operations, and the database is PostgreSQL paired with SQLAlchemy.
#### Also added Redis for caching
**launch**: ```bash docker compose up -d```
